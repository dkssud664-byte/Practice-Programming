
# 정수 내림차순으로 배치하기

링크 : https://school.programmers.co.kr/learn/courses/30/lessons/12933

###### 문제 설명

함수 solution은 정수 n을 매개변수로 입력받습니다. n의 각 자릿수를 큰것부터 작은 순으로 정렬한 새로운 정수를 리턴해주세요. 예를들어 n이 118372면 873211을 리턴하면 됩니다.

##### 제한 조건

-   `n`은 1이상 8000000000 이하인 자연수입니다.

##### 입출력 예

|n|return|
|-|-|
|118372|873211|


---
**실행 1(실패)**
생각 : n을 문자열로 변환 후 각 문자를 인트배열에 넣고 내림차순으로 정렬 한 뒤 mul을 곱하면서 answer에 값을 넣는다.

결과 : 실패
테스트1,2,3,4,6,8,9,11,12,13 실패

실패 이유 : int의 범위를 초과했다

```csharp
using System;

public class Solution {
    public long solution(long n) {
        long answer = 0;

        int[] nums = new int[n.ToString().Length];
        string str = n.ToString();
        int mul = 1;

        for(int i = 0; i < nums.Length; i++)
        {
            nums[i] = int.Parse(str[i].ToString());
        }

        Array.Sort(nums);

        for(int i = 0; i < nums.Length; i++)
        {
            answer += nums[i] * mul;
            mul *= 10;
        }

        return answer;
    }
}
```
---
**실행 2(성공)**
생각 : int배열을 long으로 바꾼다
결과 : 성

```csharp
using System;

public class Solution {
    public long solution(long n) {
        long answer = 0;

        long[] nums = new long[n.ToString().Length];
        string str = n.ToString();
        int mul = 1;

        for(int i = 0; i < nums.Length; i++)
        {
            nums[i] = int.Parse(str[i].ToString());
        }

        Array.Sort(nums);

        for(int i = 0; i < nums.Length; i++)
        {
            answer += nums[i] * mul;
            mul *= 10;
        }

        return answer;
    }
}
```

---
**후기**
타입에 유의하자