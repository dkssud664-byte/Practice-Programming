
# 시저 암호

링크 : https://school.programmers.co.kr/learn/courses/30/lessons/12926


###### 문제 설명

어떤 문장의 각 알파벳을 일정한 거리만큼 밀어서 다른 알파벳으로 바꾸는 암호화 방식을 시저 암호라고 합니다. 예를 들어 "AB"는 1만큼 밀면 "BC"가 되고, 3만큼 밀면 "DE"가 됩니다. "z"는 1만큼 밀면 "a"가 됩니다. 문자열 s와 거리 n을 입력받아 s를 n만큼 민 암호문을 만드는 함수, solution을 완성해 보세요.

##### 제한 조건

-   공백은 아무리 밀어도 공백입니다.
-   s는 알파벳 소문자, 대문자, 공백으로만 이루어져 있습니다.
-   s의 길이는 8000이하입니다.
-   n은 1 이상, 25이하인 자연수입니다.

##### 입출력 예

|s|n|result|
|-|-|-|
|"AB"|1|"BC"|
|"z"|1|"a"|
|"a B z"|4|"e F d"|




---
**실행 1(실패)**
생각 :  각 문자를 아스키 코드의 숫자로 생각하고 n만큼 더해준다.

결과 : 실패

실패 이유  : answer에 숫자가 들어갔다.


```csharp
public class Solution {
    public string solution(string s, int n) {
        string answer = "";

         for(int i = 0; i < s.Length; i++)
         {
             if ('A' <= s[i] && s[i] <= 'Z')
             {
                 if (s[i] + n > 'Z')
                 {
                     answer += s[i] + n - 'A';
                 }
                 else
                 {
                     answer += s[i] + n;
                 }
             }
             else if('a' <= s[i] && s[i] <= 'z')
             {
                 if (s[i] + n > 'z')
                 {
                     answer += s[i] + n - 'a';
                 }
                 else
                 {
                     answer += s[i] + n;
                 }
             }

         }

         return answer;
    }
}
```


---
**실행 2(실패)**
생각 :  아스키코드를 계산한 결과를 Tostring()으로 감싸준다

결과 : 실패

실패 이유  : 여전히 answer에 숫자가 들어갔다.


```csharp
public class Solution {
    public string solution(string s, int n) {
        string answer = "";

        for(int i = 0; i < s.Length; i++)
        {
            if ('A' <= s[i] && s[i] <= 'Z')
            {
                if (s[i] + n > 'Z')
                {
                    answer += (s[i] + n - 'A').ToString();
                }
                else
                {
                    answer += (s[i] + n).ToString();
                }
            }
            else if('a' <= s[i] && s[i] <= 'z')
            {
                if (s[i] + n > 'z')
                {
                    answer += (s[i] + n - 'a').ToString(); ;
                }
                else
                {
                    answer += (s[i] + n).ToString();
                }
            }
        }

        return answer;
    }
}
```

---
**실행 3(실패)**

생각 : 캐스팅을 한다.
결과 : 실패
실패 이유 : 계산식이 틀렸다.

---
**실행 4(실패)**

생각 : z나 Z라면 직접 26을 빼준다
결과 : 실패
실패 이유 : 띄어쓰기를 고려 안했다.

---
**실행 5(성공)**

생각 : 대문자 소문자가 아니면 띄어쓰기를 한다
결과 : 성공
```csharp
public class Solution {
    public string solution(string s, int n) {
        string answer = "";

        for(int i = 0; i < s.Length; i++)
        {
            if ('A' <= s[i] && s[i] <= 'Z')
            {
                if (s[i] + n > 'Z')
                {

                    answer += (char)(s[i] + n - 26);
                }
                else
                {
                    answer += (char)(s[i] + n);
                }
            }
            else if('a' <= s[i] && s[i] <= 'z')
            {
                if (s[i] + n > 'z')
                {
                    answer += (char)(s[i] + n - 26); ;
                }
                else
                {
                    answer += (char)(s[i] + n);
                }
            }
            else
            {
                answer += " ";
            }
        }

        return answer;
    }
}
```

---
**후기**

문제를 읽고 쉬운 문제라고 생각하고 풀어서 예외처리나 기본적인 계산을 잘 처리하지 못해 5번이나 수정을 거듭했다.

좀 더 꼼꼼히 생각하자