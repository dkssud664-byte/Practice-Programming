
# 제일 작은 수 제거하기

링크 : https://school.programmers.co.kr/learn/courses/30/lessons/12935

###### 문제 설명

정수를 저장한 배열, arr 에서 가장 작은 수를 제거한 배열을 리턴하는 함수, solution을 완성해주세요. 단, 리턴하려는 배열이 빈 배열인 경우엔 배열에 -1을 채워 리턴하세요. 예를들어 arr이 [4,3,2,1]인 경우는 [4,3,2]를 리턴 하고, [10]면 [-1]을 리턴 합니다.

##### 제한 조건

-   arr은 길이 1 이상인 배열입니다.
-   인덱스 i, j에 대해 i ≠ j이면 arr[i] ≠ arr[j] 입니다.

##### 입출력 예

|arr|return|
|-|-|
|[4,3,2,1]|[4,3,2]|
|[10]|[-1]|


---
**실행 1(실패)**
생각 : List에 arr의 가장 작은 수를 제외하고 넣어놓고 ToArray로 반환한다. 
결과 : 실패
실패 이유 : 테스트 케이스 1번 시간초과
foreach를 돌릴때 내부에서 arr.Min()이 연산을 오래 잡는 것 같다.

```csharp
using System.Collections.Generic;
using System.Linq;

public class Solution {
    public int[] solution(int[] arr) {
        List<int> list = new List<int>();
        int min = arr.Min();
        
        foreach(int i in arr)
        {
            if(i == arr.Min())
            {
                continue;
            }
            list.Add(i);
        }
        
        if(list.Count == 0)
        {
            int[] answer = new int[] {-1};
            return answer;
        }

        
        return list.ToArray();
    }
}
```
---
**실행 2(성공)**
생각 : arr.Min()을 다른 변수에 담아놓고 반복문을 돌린다
결과 : 성공

```csharp
using System.Collections.Generic;
using System.Linq;

public class Solution {
    public int[] solution(int[] arr) {
        List<int> list = new List<int>();
        int min = arr.Min();
        
        foreach(int i in arr)
        {
            if(i == min)
            {
                continue;
            }
            list.Add(i);
        }
        
        if(list.Count == 0)
        {
            int[] answer = new int[] {-1};
            return answer;
        }

        
        return list.ToArray();
    }
}
```

---
**후기**
고정된 숫자의 역할을 하는 함수를 쓸 때는 밖에서 미리 담아놓고 사용해야겠다.
