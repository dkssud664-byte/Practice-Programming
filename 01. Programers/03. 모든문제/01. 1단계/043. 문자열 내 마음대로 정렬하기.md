
# 문자열 내 마음대로 정렬하기

링크 : https://school.programmers.co.kr/learn/courses/30/lessons/12915

###### 문제 설명

문자열로 구성된 리스트 strings와, 정수 n이 주어졌을 때, 각 문자열의 인덱스 n번째 글자를 기준으로 오름차순 정렬하려 합니다. 예를 들어 strings가 ["sun", "bed", "car"]이고 n이 1이면 각 단어의 인덱스 1의 문자 "u", "e", "a"로 strings를 정렬합니다.

##### 제한 조건

-   strings는 길이 1 이상, 50이하인 배열입니다.
-   strings의 원소는 소문자 알파벳으로 이루어져 있습니다.
-   strings의 원소는 길이 1 이상, 100이하인 문자열입니다.
-   모든 strings의 원소의 길이는 n보다 큽니다.
-   인덱스 1의 문자가 같은 문자열이 여럿 일 경우, 사전순으로 앞선 문자열이 앞쪽에 위치합니다.

##### 입출력 예

|strings|n|return|
|-|-|-|
|["sun", "bed", "car"]|1|["car", "bed", "sun"]|
|["abce", "abcd", "cdx"]|2|["abcd", "abce", "cdx"]|

##### 입출력 예 설명

**입출력 예 1**  
"sun", "bed", "car"의 1번째 인덱스 값은 각각 "u", "e", "a" 입니다. 이를 기준으로 strings를 정렬하면 ["car", "bed", "sun"] 입니다.

**입출력 예 2**  
"abce"와 "abcd", "cdx"의 2번째 인덱스 값은 "c", "c", "x"입니다. 따라서 정렬 후에는 "cdx"가 가장 뒤에 위치합니다. "abce"와 "abcd"는 사전순으로 정렬하면 "abcd"가 우선하므로, 답은 ["abcd", "abce", "cdx"] 입니다.


---
**실행 1(실)**
생각 : n의 인덱스의 값을 찾고 인덱스의 값으로 오름차순 정렬한다.

결과 : 1,2,9 제외 실패
실패 이유 : 불필요한 스왑이 많은 것 같다.

```csharp
public class Solution {
    public string[] solution(string[] strings, int n)
        {
            int length = strings.Length;
            string[] answer = new string[length];
            char[] chars = new char[length];

            //n번째 문자 넣기
            for(int i = 0; i < length; i++)
            {
                chars[i] = strings[i][n];
            }

            //n번째 문자와 비교
            for(int i = 0; i < length; i++)
            {
                bool isSwap = false;
                for(int j = 0; j < length - 1 - i; j++)
                {
                    //비교 인덱스가 크면 스왑
                    if (chars[j] > chars[j + 1])
                    {
                        Swap(strings, j, j + 1);
                        Swap(chars, j, j + 1);
                        isSwap = true;
                    }
                    //비교 인덱스가 같으면
                    else if(chars[j] == chars[j + 1])
                    {
                        bool isRight = false;
                        bool isDiff = false;

                        int temp = strings[j].Length <= strings[j + 1].Length ? strings[j].Length : strings[j + 1].Length;

                        //길이가 같으면 전체를 비교
                        for(int k = 0; k < temp; k++)
                        {
                            if(k == n)
                            {
                                continue;
                            }

                            if (strings[j][k] > strings[j + 1][k])
                            {
                                Swap(strings, j, j + 1);
                                Swap(chars, j, j + 1);
                                break;
                            }
                        }

                        //길이가 작은 값을 앞으로
                        if (strings[j].Length > strings[j + 1].Length)
                        {
                            Swap(strings, j, j + 1);
                            Swap(chars, j, j + 1);
                            continue;
                        }
                    }
                }

                if(!isSwap)
                {
                    break;
                }
            }

            answer = strings;

            return answer;
        }

        public void Swap(string[] strs, int a, int b)
        {
            string temp = strs[a];
            strs[a] = strs[b];
            strs[b] = temp;
        }

        public void Swap(char[] chars, int a, int b)
        {
            char temp = chars[a];
            chars[a] = chars[b];
            chars[b] = temp;
        }
}
```
---
**실행 1(성공)**
생각 : 사전으로 미리 정렬을 한 후 인덱스로 정렬한다.

결과 : 성공

```csharp
using System;

public class Solution {
    public string[] solution(string[] strings, int n)
        {
            int length = strings.Length;
            string[] answer = new string[length];
            char[] chars = new char[length];

            //n번째 문자 넣기
            for(int i = 0; i < length; i++)
            {
                chars[i] = strings[i][n];
            }

            //사전순 정렬
            for(int i = 0; i < strings.Length; i++)
            {
                for(int j = 0; j < length - 1 - i; j++)
                {
                    int tempLength = Math.Min(strings[j].Length, strings[j + 1].Length);
                    int isLongLength = strings[j].Length > strings[j + 1].Length ? 1 : 0;
                    bool isSmall = false;
                    bool isSame = true;

                    for(int k = 0; k < tempLength; k++)
                    {
                        if (strings[j][k] > strings[j + 1][k] && isSame)
                        {
                            isSmall = true;
                            break;
                        }
                        else if (strings[j][k] < strings[j + 1][k])
                        {
                            isSame = false;
                        }
                    }

                    if(isSmall)
                    {
                        Swap(strings, j, j + 1);
                        Swap(chars, j, j + 1);
                    }
                    else if(isSame)
                    {
                        if(isLongLength == 1)
                        {
                            Swap(strings, j, j + 1);
                            Swap(chars, j, j + 1);
                        }
                    }
                }
            }

            //인덱스기준 문자 정렬
            for(int i = 0; i < chars.Length; i++)
            {
                for(int j = 0; j < chars.Length - i - 1; j++)
                {
                    if (chars[j] > chars[j + 1])
                    {
                        Swap(strings, j, j + 1);
                        Swap(chars, j, j + 1);
                    }
                }
            }

            return answer = strings;
        }

        public void Swap(string[] strs, int a, int b)
        {
            string temp = strs[a];
            strs[a] = strs[b];
            strs[b] = temp;
        }

        public void Swap(char[] chars, int a, int b)
        {
            char temp = chars[a];
            chars[a] = chars[b];
            chars[b] = temp;
        }
}
```

---
**후기**
단순한 버블정렬을 사용해 정렬을 했는데 string으로 하니 잔실수가 많았다